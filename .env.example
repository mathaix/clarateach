# ClaraTeach Development Configuration

# Portal API
PORTAL_PORT=3000
PORTAL_HOST=0.0.0.0

# Frontend
FRONTEND_PORT=5173

# Caddy Reverse Proxy (workspace stack)
CADDY_PORT=8000

# Workspace endpoint for local development (Caddy proxy URL)
# This overrides the Docker internal IP that browsers can't reach
WORKSPACE_ENDPOINT_OVERRIDE=http://localhost:8000

# Workspace Server (inside container, proxied by Caddy)
WORKSPACE_PORT=3001

# Neko Browser
NEKO_PORT=8080

# JWT Authentication
# JWKS URL for workspace containers to validate tokens (use host.docker.internal for Mac)
JWKS_URL=http://host.docker.internal:3000/.well-known/jwks.json

# ============================================
# GCP VM Provisioning (for production)
# ============================================
# Set GCP_PROJECT to enable GCP VM provisioning
# Leave empty for local Docker-only mode

# Your GCP project ID
# GCP_PROJECT=my-gcp-project

# GCE zone for VMs (default: us-central1-a)
# GCP_ZONE=us-central1-a

# Artifact Registry URL for workspace container image
# GCP_REGISTRY=us-central1-docker.pkg.dev/my-project/clarateach

# Use Spot/Preemptible VMs for testing (cheaper, can be terminated)
# GCP_USE_SPOT=true

# Note: GCP authentication uses Application Default Credentials
# Run: gcloud auth application-default login
