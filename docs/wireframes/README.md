
  # Webpage with VM Integration

  This is a code bundle for Webpage with VM Integration. The original project is available at https://www.figma.com/design/yRKDhWymPmjBP0KbmtyndS/Webpage-with-VM-Integration.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  